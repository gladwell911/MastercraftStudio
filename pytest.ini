[pytest]
testpaths =
    tests
    test
    test_program_runner.py
python_files = test_*.py
markers =
    live: opt-in tests that hit real external services (e.g. KIMI_LIVE_TEST=1)
norecursedirs =
    .git
    .hg
    .svn
    .venv
    build*
    dist
    tmp*
    .worktrees
    codex-home-*
