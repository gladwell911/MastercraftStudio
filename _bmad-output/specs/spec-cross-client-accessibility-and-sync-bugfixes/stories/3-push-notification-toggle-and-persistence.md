---
status: draft
---

# 推送通知开关与持久化

## Auto Run Result

Status: draft
Previous blocking condition: dirty working tree (`rc`: modified `pubspec.lock`; untracked `.agents/` and `_bmad/`). Reset for a new run after the project changes are committed.
