# 当前交接

## 快照（2026-09-10）

当前分支为 `feature/epic-2-cross-client-sessions`。Epic 2 的 Story 2.1、2.2、2.3 已完成；下一项是 Story 2.4（同步完整执行时间线到移动端）。

## 已完成

- Story 2.1：规范化 owner/session 身份，增加 V2 协议协商、持久事实、outbox、epoch、哈希去重和隔离。
- Story 2.2：实现 revision 化权威清空、原子状态机、首消息 exactly-once 重发和失败恢复。
- Story 2.3：MC 在 state/history/clear 响应中提供隐私安全的 revision 与最新 clear authority；权威读取保持原子一致。
- MC/RC 使用字节一致的 V2 可执行契约矩阵，覆盖 12 个清空对账场景。

## 验证状态

- Story 2.3 定向 MC 测试：12 通过；两个新增响应字段的回归断言修正后单独通过。
- 完整 MC 回归最近一次运行：1692 通过、5 跳过；两个契约断言修正后，剩余失败面与已知 102 项基线一致。
- `git diff --check` 通过。
- 无可用模拟器或实体设备，因此真实跨设备 NATS 清空链路未执行。

## 下一步

1. 按 Sprint 状态开始 Story 2.4。
2. 延续 revision-first、owner 隔离、持久事实和共享契约矩阵约束。
3. 有设备时补跑 Story 2.2/2.3 的真实跨端清空、重连和历史刷新验证。

## 风险与不要重复尝试

- 完整测试仍有历史基线失败，不能把定向测试通过描述为全仓全绿。
- 不要用空历史外观推断清空，也不要在移动端本地重发首消息。
- 当前分支没有配置 Git upstream；推送前必须由维护者明确配置，不能猜测远端目标分支。
