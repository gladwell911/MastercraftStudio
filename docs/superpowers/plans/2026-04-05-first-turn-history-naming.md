# First-Turn History Naming Implementation Plan  > **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.  **Goal:** Create and name a history chat only after the first turn finishes, using a model-generated summary of the first user question, with a deterministic fallback if the summary fails.  **Architecture:** The current session will stay untouched until the first turn resolves. At that point, the app will create one archived chat entry from the session snapshot, derive its title from the first question only, and refresh the history list. If title generation fails, a local fallback will synthesize a short title so the archived entry never remains unnamed. The implementation should preserve existing history persistence, deletion, and selection behavior.  **Tech Stack:** Python 3, wxPython, pytest  ---  ### Task 1: Lock in the first-turn archive behavior with tests  **Files:** - Modify: `tests/test_main_unit.py`  - [ ] **Step 1: Write the failing test**  ```python def test_first_turn_completion_creates_archived_chat_with_summary_title(frame, monkeypatch):     frame.active_chat_id = "chat-active"     frame.current_chat_id = "chat-active"     frame._current_chat_state["id"] = "chat-active"     frame.active_session_turns = [         {             "question": "帮我把这个页面改成深色风格并保持按钮可点击",             "answer_md": main.REQUESTING_TEXT,             "model": "codex/main",             "created_at": 1.0,             "request_status": "pending",             "request_recoverable": True,         }     ]     frame.active_turn_idx = 0     frame.archived_chats = []     monkeypatch.setattr(frame, "_summarize_recent_topic", lambda turns, api_key: "深色风格改造")     monkeypatch.setattr(frame, "_save_state", lambda: None)     monkeypatch.setattr(frame, "_refresh_history", lambda *args, **kwargs: None)      frame._on_done(0, "已完成", "", "codex/main", "", "chat-active")      assert frame.archived_chats     archived = frame.archived_chats[0]     assert archived["title"] == "深色风格改造"     assert archived["turns"][0]["question"] == "帮我把这个页面改成深色风格并保持按钮可点击" ```  - [ ] **Step 2: Run the test to verify it fails**  Run: `.\\.venv311\\Scripts\\python.exe -m pytest tests/test_main_unit.py -k "first_turn_completion_creates_archived_chat_with_summary_title" -q` Expected: FAIL because the chat is not archived on first-turn completion yet.  - [ ] **Step 3: Write the fallback-title test**  ```python def test_first_turn_archive_uses_local_fallback_when_summary_fails(frame, monkeypatch):     frame.active_chat_id = "chat-active"     frame.current_chat_id = "chat-active"     frame._current_chat_state["id"] = "chat-active"     frame.active_session_turns = [         {             "question": "请把这个问题总结成一个简短标题",             "answer_md": main.REQUESTING_TEXT,             "model": "codex/main",             "created_at": 1.0,             "request_status": "pending",             "request_recoverable": True,         }     ]     frame.active_turn_idx = 0     frame.archived_chats = []     monkeypatch.setattr(frame, "_summarize_recent_topic", lambda turns, api_key: "")     monkeypatch.setattr(frame, "_save_state", lambda: None)     monkeypatch.setattr(frame, "_refresh_history", lambda *args, **kwargs: None)      frame._on_done(0, "", "boom", "codex/main", "", "chat-active")      assert frame.archived_chats     assert frame.archived_chats[0]["title"] == "请把这个问题总结成一个简短标题" ```  - [ ] **Step 4: Run the tests to verify they fail for the right reason**  Run: `.\\.venv311\\Scripts\\python.exe -m pytest tests/test_main_unit.py -k "first_turn_completion_creates_archived_chat_with_summary_title or first_turn_archive_uses_local_fallback_when_summary_fails" -q` Expected: FAIL because first-turn completion does not yet create an archived chat with the desired title behavior.  ### Task 2: Implement first-turn archiving and title generation  **Files:** - Modify: `main.py`  - [ ] **Step 1: Add a helper that archives the first turn once it resolves**  ```python def _archive_first_turn_after_completion(self, chat_id: str, used_model: str, err: str) -> None:     # If this is the first turn and we have not created a history entry yet,     # snapshot the session into archived_chats and derive the title from the first question.     ... ```  - [ ] **Step 2: Call the helper from `_on_done`**  ```python if turn_idx == 0 and not self._find_archived_chat(chat_id or self.active_chat_id or self.current_chat_id or ""):     self._archive_first_turn_after_completion(chat_id or self.active_chat_id or self.current_chat_id or "", used_model, err) ```  - [ ] **Step 3: Ensure title generation uses only the first question**  ```python def _summarize_first_question_only(self, question: str, api_key: str) -> str:     ... ```  - [ ] **Step 4: Fall back locally when the model summary fails**  ```python title = self._summarize_first_question_only(first_question, api_key) if not title:     title = self._summarize_first_question_locally(first_question) ```  - [ ] **Step 5: Run the focused tests**  Run: `.\\.venv311\\Scripts\\python.exe -m pytest tests/test_main_unit.py -k "first_turn_completion_creates_archived_chat_with_summary_title or first_turn_archive_uses_local_fallback_when_summary_fails" -q` Expected: PASS  ### Task 3: Verify the full history flow  **Files:** - Modify: `tests/test_main_unit.py`  - [ ] **Step 1: Add or adjust a startup test to ensure cleared history stays cleared**  ```python def test_startup_does_not_resurrect_cleared_history_from_legacy_files(...):     ... ```  - [ ] **Step 2: Run the full main unit suite**  Run: `.\\.venv311\\Scripts\\python.exe -m pytest tests/test_main_unit.py -q` Expected: PASS  - [ ] **Step 3: Review the diff for only the intended files**  Run: `git diff -- main.py tests/test_main_unit.py docs/superpowers/plans/2026-04-05-first-turn-history-naming.md` 