# 本轮反思

| 修改内容 | 错误归因 | 下次指令建议 |
|---|---|---|
| 冷启动测试曾关闭并重新启动用户正在使用的 `D:\code\cx\mc\mc.exe`，但未提前通知。程序未崩溃且文件未被替换，但造成了可见干扰。 | 判断逻辑有问题 | 任何会关闭、重启或切换用户桌面程序的测试，在执行前必须明确告知程序路径、影响和预计持续时间；用户未确认时改用隔离 harness。 |
